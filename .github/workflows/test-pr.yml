name: Test Pull Request

on:
  pull_request:
    paths:
      - 'package.nix'
      - 'flake.nix'
      - 'flake.lock'
      - 'wrapper/**'
      - '.github/workflows/**'

jobs:
  test-build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
        env:
          NIX_FIRST_BUILD_UID: 400

      - name: Setup Nix cache
        uses: cachix/cachix-action@v15
        with:
          name: gemini-cli-nix-cache
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
          skipPush: true

      - name: Build package
        run: |
          nix build .#gemini-cli --print-build-logs

      - name: Test gemini version
        run: |
          # Test that gemini runs and reports version
          ./result/bin/gemini --version

      - name: Verify package contents
        run: |
          # Verify the binary exists
          test -f ./result/bin/gemini

      - name: Run basic functionality test
        run: |
          # Test help command
          ./result/bin/gemini --help || true

          # Verify it's the correct version
          VERSION=$(grep 'version = ' package.nix | head -1 | cut -d'"' -f2)
          ./result/bin/gemini --version 2>&1 | grep -E "$VERSION"
